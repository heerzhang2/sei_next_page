关键改进：只有当 `modified=true` 时才保存到IndexedDB，避免保存未修改的数据；在组件挂载时立即从IndexedDB加载用户编辑的数据（只加载 `modified=true` 的数据），并恢复 `modified` 状态
；在ReportData中增强检查逻辑，不仅检查 `modified` 状态，还检查本地storage是否已有数据，
避免网络数据覆盖用户编辑或IndexedDB恢复的数据。这样确保用户的编辑在导航时被保存和恢复，不会被网络数据覆盖。