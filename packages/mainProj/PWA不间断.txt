官方文档 https://serwist.pages.dev/docs/serwist/runtime-caching/plugins
不用生产版"start:https": "cross-env NODE_ENV=production "真的无法发挥PWA的全部功能。
cacheName: "next-chunks", 静态资源,DEV模式才用? 但是dev没法用serwist没页面只显示rsc代码;
重新build导致的 /_next/static/开头资源，要求任何模板任意版本都需要重新做缓存的，否则无法确保能够访问。
【证书信任】PS D:\home\sei_next_page\packages\mainProj\ssl> python -m http.server 8000    开发本机:受信任的根证书颁发机构
苹果safari手机访问   http://192.168.0.100:8000/ca.cer  Safari 会自动识别 .cer 文件并弹出“安装描述文件”提示 → 点击 “允许” → “安装” → 输入锁屏密码。
打开 设置（Settings）进入：通用（General） > 关于本机（About）滚动到底部 → 点击 “证书信任设置”（如果看不到这一项，说明你安装的不是根证书，或格式不对）在 “针对根证书启用完全信任” 列表中，找到你的 CA 名称（如 localhost 或 MyDevCA）打开右侧开关，启用信任。
【urql offlinecahce问题】版本：
1003 17：36； optimistic可能遭遇退回。第一次保存丢失，刷新后显示不一致，同步机会拖延，第一次的编辑区丢失更新。
0928  10：21； 刷新较小的可能导致丢失，cache估计也没了，最后复原了。
0926  06：30； 失效token、401导致?，有极小概率显示不一致的。小概率丢失:2个少了一个mutation。
1006---保存后死循环，刷新丢失列表。
      保存后期间若离线或401，可能导致第一次的mutation丢失。小概率丢失更新列表1/2个的。

## 问题1: 为什么 #html 会变成 #rsc？

**根本原因**：Cache API **不支持 URL fragment（#）作为缓存键的区分**！

当你存储：

- `https://example.com/page#html`
- `https://example.com/page#rsc`


Cache API 会**忽略 fragment 部分**，把它们当作**同一个键**！所以第二次 `cache.put()` 会覆盖第一次的内容。这就是为什么 `#html` 被 `#rsc` 覆盖的原因。

**解决方案**：使用**查询参数**而不是 fragment 来区分 HTML 和 RSC。

## 问题2: 为什么需要自定义处理 CACHE_URLS？

Serwist 的内置消息处理只针对 **precache manifest**（`__SW_MANIFEST`），不支持：

1. 运行时动态缓存用户指定的 URLs
2. 同时缓存 HTML 和 RSC 两个版本
3. 自定义缓存键标准化
4. 错误处理和进度报告


所以必须自定义代码来实现这些功能。

## 问题3: SPA vs Next.js PWA

**是的！SPA 的 PWA 实现简单得多**：

- ✓ 只有一个版本的页面（HTML + JS）
- ✓ 不需要区分 HTML 和 RSC
- ✓ 缓存策略简单直接
- ✓ 没有服务端渲染的复杂性


Next.js RSC 增加了复杂度，因为需要管理两个版本的页面和它们之间的关系。

Chrome Cache Storage 限制详解
全局共享限制
6% 的磁盘空间：是所有网站 Cache Storage 的总容量

共享池：所有网站的 Service Worker 缓存都从这个池中分配

竞争关系：如果一个网站使用了大量缓存，其他网站的可用空间就会减少

单个网站的实际限制
虽然理论上没有硬性的单站限制，但实际上：

动态分配：Chrome 根据网站使用频率和重要性动态分配

LRU 策略：最少使用的缓存会被优先清理

无保证配额：单个网站无法保证能用到特定大小的缓存
缓存清理策略
当达到全局限制时，Chrome 会：

自动清理：基于 LRU（最近最少使用）算法

跨站清理：可能清理其他网站的缓存

无预警：不会通知网站缓存被清理
主动管理：定期清理旧缓存，避免被系统自动清理重要数据
【失去效果】
Q: 能否只缓存变化的模板？
A: 可以！使用 changeTime 机制和"自动更新"功能，只更新实际修改的模板。但 Next.js 的共享 chunks 可能仍然需要更新。
发送去主线程是=:
url
:
"https://192.168.171.3:3765/_next/static/chunks/app/layout-4f5a4b6730e94150.js"

[主线程]收到SW的SWChunk==
error
:
"资源加载失败: https://192.168.171.3:3765/rep/wjSpD8qsRvGy-zmji0lUK1JlcG9ydA/SLIDING_JJ/1/T6-14?_rsc=3lb4g#T6-14"


__Host-authjs.csrf-token=7d9deb9afa19fb298360ac304c23a65b0617b431fa47edd03a396dd94b63f862%7C51ae85798add3e3690d92972e8bca7ab3b4d11f75442293ee44dd374fc387a04; __Secure-authjs.callback-url=https%3A%2F%2F192.168.0.100%3A3765%2Flogin; __next_hmr_refresh_hash__=6aceba72def92d0c21156ec139f5cff1c34a80d9a05dfe66; __Secure-authjs.session-token=eyJhbGciOiJkaXIiLCJlbmMiOiJBMjU2Q0JDLUhTNTEyIiwia2lkIjoiWEF2ZGRyWGV3c25jQ0V5WE85XzVQZ1oyQ1pNeFVSZGVYZXBiSDF0c0V6cnNxbW9iY0owQ1N5Z1pSdEhrajlMOUdXZFk4Vndpc2pKbmh4dFlxVzZ6dUEifQ..Y6SXYAr-05X5_4Xf0p4oaw.7hxkc5PovEnY53ZD3mQYm4eLpv1VINRmS5wIxyGU9W0IsXiG1jbVufsfZJb9RnUw0Qmg1S7LxSzteOC3QR_3SjMJu9cNXnkhio0aS4pWxpL-X9HLdvEv09B9E70Zw25__Zjck_Z3TtNIuX3BcuelG1iZP-hlO3MZ0gMAfMjhwzbJnOyH1ti3lEnC-dl4DgM_hQb6CffuMzjxNT8TxMk_1n-Zc9fCJN7YeSq1amUKNHer5s0aobptrSeB8fNAwxfM99P-NICHstHZs92FcSXjOErPZttfhRR4flbky0GDCsDYKFQfQ1Gm_eFaNHk21w0vK7DB6M9jE8F6chHgERNlmwN6k1YMZHK86qVnQZp2F1n7oA_D5nN5RFDWEPUXjTyZmpNoo44nsB3j-ofTBiI1O2y6kPUJxHlIz0pqHNdOawseXfIkBrwT5jhR1y6vNtQtgDTwx7k-V-kMLV1CWKr3zGCLOOxsGX4bnUMQ0Xm1pak8iEjgz254z58eAIPxofuSPfaDReYovhSFPyubmM62u0-wE0KsoyUmBDgaFrI_ywDuI1WAnV83y8qlskI0zJibSQyDX9q77xyba2fJhUhvgJnGj1V-HUeX7bwIgQwt6CE.GRM9faxiK5FiJny1F3s5WtQeeRaC61o6Er4BG8IV79s
host
192.168.0.100:3765
https://127.0.0.1:13501/browser/ywmast/202511%2F0305%2Fc5e9bdab-688b-494f-9bae-5156f0a406f6.avif
https://127.0.0.1:9000/ywmast/202511/0305/0ae846dc-ac49-4b79-8469-ca6d0f64f62f.png

https://192.168.0.100:8673/uploadTUS/f53e6b82-b92c-41b5-a2fc-b75633113bb3 xhr
CORS错误



Name:
14a84434-c76c-4cdf-bb4a-ed806330c2b7
Size:
2.9 MiB
Versions:
1 version, 2.9 MiB
Last Modified:
1 minute ago
ETAG:
afb61bffe7d1d40e8e974d31da0827a6
Tags:
N/A
Legal Hold:
Off
Retention Policy:
compliance
Metadata
Content-Type
application/pdf
X-Amz-Meta-Author
herzhang
X-Amz-Object-Lock-Mode
COMPLIANCE

X-Amz-Meta-Filename
=?UTF-8?b?MzUwNVkxMDAxNTIt5oql5ZGKLnBkZg==?=
X-Amz-Meta-Rep
yAEq8hveSa-ZXgUyKHEEHVJlcG9ydA

X-Amz-Object-Lock-Retain-Until-Date
2025-11-13T08:28:07.816Z
String liveDays=metas.getOrDefault("liveDays","1");
uppyInstance.setMeta({ rep: repId, liveDays })

eid: repId, liveDays, business

集群环境下，TUS 协议的 请求确实可能因为负载均衡而出现问题， 解决方案，会话超时时间足够长（≥ 文件上传最大预计时间）
方案 1：会话亲和性（推荐）
配置负载均衡器的 会话亲和性（Session Affinity）：
假如# Nginx 配置 nginx
upstream backend_servers {
    ip_hash;  # 基于客户端 IP 的会话保持
    server 192.168.1.10:8080;
    server 192.168.1.11:8080;
}

# 或者基于 cookie 的会话保持
upstream backend_servers {
    server 192.168.1.10:8080;
    server 192.168.1.11:8080;
    sticky cookie srv_id expires=1h domain=.example.com path=/;
}
假如# Kubernetes 配置  yaml
apiVersion: v1
kind: Service
metadata:
  name: tus-backend
spec:
  selector:
    app: tus-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  sessionAffinity: ClientIP  # 基于客户端 IP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800  # 3小时
【增强模块】@uppy/golden-retriever 插件会将选中的文件保存在浏览器缓存中，这样即使浏览器崩溃或用户意外关闭标签页，Uppy也能恢复所有内容并继续上传，就像什么都没发生过一样。
Upload complete
Complete
Done


https://192.168.171.3:8673/uploadTUS/

authorization
Bearer eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJoZXJ6aGFuZyIsImlhdCI6MTc2MjMwOTc1NCwiZGV2aWNlSWQiOiJlMnM4dGx5IiwiZXhwIjoxNzYyMzExNzU0fQ.0jnt30K4y67Fu7VxcSHopIu3-xF-eL9B38ZVsLIDMHLG74uUwv4gfrsVWftx20DB-uy4DUQKxcvMlVpHMlWdPg

upload-metadata
eid eUFFcThodmVTYS1aWGdVeUtIRUVIVkpsY0c5eWRB,liveDays MTA=,business cmVw,relativePath bnVsbA==,name 5Y6f5aeL6K6w5b2VKDAxKS5wZGY=,type YXBwbGljYXRpb24vcGRm,filetype YXBwbGljYXRpb24vcGRm,filename 5Y6f5aeL6K6w5b2VKDAxKS5wZGY=

tus-resumable
1.0.0
upload-length
1069974


curl -X POST https://192.168.171.3:8673/uploadTUS/ -H "Accept: application/json"  -H "Authorization: Bearer eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJoZXJ6aGFuZyIsImlhdCI6MTc2MjMxMjI3NywiZGV2aWNlSWQiOiJlMnM4dGx5IiwiZXhwIjoxNzYyMzE0Mjc3fQ.wrXNrENlhSH3o_3BXlLkQxEXCuZqO3Tt14cBAYYQMofFjt4greMWdXxekaf9uuSJiDfWRBSGXuvTABc3Vo6S0A" -H "Tus-Resumable: 1.0.0"  -H "Upload-Length: 1024"  --http1.1  -k -v
[文件离线问题]**通用回调处理器**方案
序列化回调参数而不是函数本身。系统会在队列中存储 `modType`、`redId`、`fieldPath` 等参数，从 `/offline` 页面处理队列时，通用处理器根据这些参数直接更新 storageContext，无需导航到原始编辑器页面。这样既保持了灵活性，又避免了复杂的页面跳转逻辑。
负载均衡的TUS协议问题：类比的@tus/server; https://github.com/tus/tusd/blob/main/examples/nginx.conf
        # Disable request and response buffering
        proxy_request_buffering  off;
        # Add X-Forwarded-* headers
        proxy_set_header X-Forwarded-Host $host:$server_port;   头部标识扩展
        proxy_set_header X-Forwarded-Proto $scheme;


                // 恢复待删除操作
                if (savedState.meta?.pendingDeleteOperations && Array.isArray(savedState.meta.pendingDeleteOperations)) {
                    setPendingDeleteOperations(savedState.meta.pendingDeleteOperations)
                    console.log(`[v0] Restored ${savedState.meta.pendingDeleteOperations.length} pending delete operations`)
                }

                    //批量取消删除的函数
                    const cancelPendingOperations = useCallback(() => {
                        if (pendingDeleteOperations.length === 0) {
                            toast.info("没有待取消的删除操作")
                            return
                        }
                        setPendingDeleteOperations([])
                        toast.success("已取消所有删除操作", {
                            description: `已移除 ${Object.keys(pendingDeleteOperations).length} 个待删除文件`,
                            duration: 3000,
                        })
                    }, [pendingDeleteOperations])

                        //移除指定删除操作的函数
                        const removePendingDeleteOperations = useCallback((deleteUrls: string[]) => {
                            if (deleteUrls.length === 0) return

                            setPendingDeleteOperations((prev) => prev.filter((op) => !deleteUrls.includes(op.deleteUrl)))
                        }, [])

    React.useEffect(() => {
        if (externalPendingDeletes.length > 0) {
            console.log(`[v0] Restoring ${externalPendingDeletes.length} external pending deletes`)
            setPendingDeleteOperations((prev) => {
                const existingUrls = new Set(prev.map((op) => op.deleteUrl))
                const newOps = externalPendingDeletes.filter((op) => !existingUrls.has(op.deleteUrl))
                if (newOps.length > 0) {
                    console.log(`[v0] Adding ${newOps.length} new pending deletes to current state`)
                    return [...prev, ...newOps]
                } else {
                    console.log(`[v0] No new pending deletes to add (all already exist in current state)`)
                    return prev
                }
            })
        }
    }, [externalPendingDeletes])



//
            if (isError) {
                setPendingDeleteOperations((prev) => {
                    // 避免重复添加同一个 deleteUrl
                    if (prev.some((op) => op.deleteUrl === fileUrl)) {
                        return prev
                    }
                    return [
                        ...prev,
                        {
                            deleteUrl: fileUrl,
                            repId: eid,
                            hash: hash || "default",
                            business,
                            timestamp: Date.now(),
                        },
                    ]
                })
